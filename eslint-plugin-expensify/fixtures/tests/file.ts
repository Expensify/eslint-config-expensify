/**
 * Fixture for prefer-locale-compare-from-context tests. Path contains "tests/"
 * so isInTestFile(context.filename) is true and the rule allows localeCompare.
 * Must exist and be in tsconfig include for type-aware parsing.
 */
