/**
 * Fixture for prefer-locale-compare-from-context tests. Tests use this path
 * as filename so the rule sees a non-test file path (vs tests/file.ts for
 * isInTestFile). Must exist and be in tsconfig include for type-aware parsing.
 */
