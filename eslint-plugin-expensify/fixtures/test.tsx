/**
 * Fixture file for RuleTester tests that need JSX + parserOptions.project
 * (e.g. boolean-conditional-rendering). The linted filename is set to this path
 * so @typescript-eslint/parser finds it within the TypeScript project.
 */
